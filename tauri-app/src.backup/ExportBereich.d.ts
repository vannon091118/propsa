type Props = {
    laedt: boolean;
    onSchreiben: () => void;
};
/** Schreibt das Kontextpaket in einen Ordner. */
export declare function ExportBereich({ laedt, onSchreiben }: Props): import("react").JSX.Element;
export {};
